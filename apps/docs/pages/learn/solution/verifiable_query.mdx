# Verifiable Query
Data retrieval from off-chain state on Eastlayer is guaranteed via consensus mechanism before the data is stored on the Eastlayer Network, thus all data stored is verified and every query is verifiable.
