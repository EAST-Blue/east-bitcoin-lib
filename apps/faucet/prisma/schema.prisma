generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model History {
  id Int @id @default(autoincrement())
  address String
  createdAt Float
}